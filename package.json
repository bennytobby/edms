{
  "name": "edms",
  "version": "1.0.0",
  "description": "Electronic Document Management System",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "aws-sdk": "^2.1692.0",
    "bcrypt": "^5.1.1",
    "connect-redis": "^9.0.0",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-session": "^1.18.1",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.20.0",
    "multer": "^2.0.0-rc.4",
    "nodemailer": "^6.10.0",
    "redis": "^5.8.2"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest tests/unit/",
    "test:integration": "jest tests/integration/",
    "test:security": "jest tests/security/",
    "test:performance": "jest tests/performance/",
    "test:ci": "jest tests/ci/",
    "test:all": "npm run test:unit && npm run test:integration && npm run test:security && npm run test:performance && npm run test:ci",
    "test:cleanup": "node tests/cleanup.js",
    "test:full": "npm run test:cleanup && npm run test:all && npm run test:cleanup",
    "heroku-postbuild": "echo 'Skipping test dependencies in production'"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.1.3"
  }
}
